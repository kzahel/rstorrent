# RSTorrent Headless changelog

## [Unreleased]

No unreleased headless-service changes.

## [0.1.0]

- Add the configured ordinary-user Linux service and exact production React
  control surface.
- Add strict local-browser and private HTTPS Basic deployment modes.
- Add exact RFC 1918 `lan-none` hosting with no credentials, strict Host and
  Origin checks, and truthful exposure reporting.
- Add deterministic native x86_64 and ARM64 packages with transactional
  repair, update rollback, and preservation-safe removal.
- Add the signed-manifest release/bootstrap lane and operator-approved update
  discovery and apply path.
