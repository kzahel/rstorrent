{
  "name": "rstorrent-web",
  "version": "0.1.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check:localization": "node ../../scripts/check-localization.mjs",
    "generate:localization": "formatjs compile src/localization/messages/en.json --ast --out-file src/localization/generated/en.json",
    "extract:localization": "node scripts/migrate-inline-messages.mjs && npm run generate:localization && npm run check:localization",
    "prebuild": "npm run generate:validators && npm run generate:localization",
    "build": "vite build && npm run check:csp",
    "build:companion": "npm run generate:validators && npm run generate:localization && vite build --mode companion && npm run check:csp",
    "build:remote": "npm run generate:validators && npm run generate:localization && npm run build:remote-wasm && vite build --mode remote && node scripts/finalize-remote-dist.mjs && npm run check:csp",
    "build:remote-wasm": "node scripts/build-remote-wasm.mjs",
    "check:csp": "node scripts/check-csp-safe-dist.mjs",
    "predev": "npm run generate:validators && npm run generate:localization",
    "dev": "vite",
    "generate": "npm run generate:contract && npm run generate:validators && npm run generate:localization",
    "generate:contract": "cargo run -p rstorrent-gateway --bin export_types -- src/api/generated/v1.ts src/api/generated/v1.schema.json src/fixtures/reactive-trace.json src/fixtures/view-set-trace.json",
    "generate:validators": "node scripts/generate-validators.mjs",
    "pretest": "npm run generate:validators && npm run generate:localization",
    "test": "vitest run",
    "test:e2e": "VITE_RSTORRENT_ENABLE_PSEUDO_LOCALES=1 playwright test",
    "pretypecheck": "npm run generate:validators && npm run generate:localization",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@formatjs/icu-messageformat-parser": "3.5.17",
    "@tauri-apps/api": "2.11.1",
    "@tauri-apps/plugin-updater": "2.10.1",
    "react": "19.2.8",
    "react-aria-components": "1.20.0",
    "react-dom": "19.2.8",
    "react-intl": "10.1.25",
    "zustand": "5.0.14"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@babel/parser": "8.0.4",
    "@formatjs/cli": "6.16.22",
    "@playwright/test": "1.62.1",
    "@tauri-apps/cli": "2.11.4",
    "@testing-library/jest-dom": "7.0.0",
    "@testing-library/react": "16.3.2",
    "@testing-library/user-event": "14.6.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@types/ws": "8.18.1",
    "ajv": "8.20.0",
    "jsdom": "29.1.1",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10",
    "ws": "8.21.1"
  }
}
